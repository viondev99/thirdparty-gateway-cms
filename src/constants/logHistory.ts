export enum TABLE_NAME {
  GRANT_FEATURE_API = 'grant_feature_api',
  MODEL_API = 'model_api',
  AUTHEN_CONFIG = 'authen_config',
  FORMAT_CONFIG = 'format_config',
}

export enum LIST_MODULES {
  THIRD_PARTY = 0,
  API_FEATURE = 1,
  OUTBOUND = 2,
  ASSIGN_API_FEATURE = 3,
  DECODE = 4,
  FORMAT = 5,
}
