export type Response = {
  GET: {
    name: string
    age: number
    test: string
    isActive: boolean
    isStatus: boolean
    radio1: string
    radio2: string
    textEditor: string
  }
}

export type RequestBody = {
  POST: {
    name: string
    age: number
    test: string
    isActive: boolean
    isStatus: boolean
    radio1: string
    radio2: string
    textEditor: string
  }
}
